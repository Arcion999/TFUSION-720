# Milestone evidence

Each milestone report is the auditable gate record. A milestone is `PASS` only when every required automated and manual criterion has recorded evidence; otherwise it is `PARTIAL` or `FAIL`. Later milestone work does not begin while the preceding gate is unpassed.
